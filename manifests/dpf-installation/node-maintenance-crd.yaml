---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.15.0
  name: nodemaintenances.maintenance.nvidia.com
spec:
  group: maintenance.nvidia.com
  names:
    kind: NodeMaintenance
    listKind: NodeMaintenanceList
    plural: nodemaintenances
    singular: nodemaintenance
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NodeMaintenance is the Schema for the nodemaintenances API
        properties:
          apiVersion:
            description: APIVersion defines the versioned schema of this representation of an object.
            type: string
          kind:
            description: Kind is a string value representing the REST resource this object represents.
            type: string
          metadata:
            type: object
          spec:
            description: NodeMaintenanceSpec defines the desired state of NodeMaintenance
            properties:
              nodeName:
                description: NodeName is the name of the node to be maintained
                type: string
              reason:
                description: Reason is the reason for the maintenance
                type: string
              duration:
                description: Duration is the expected duration of the maintenance
                type: string
            required:
            - nodeName
            - reason
            type: object
          status:
            description: NodeMaintenanceStatus defines the observed state of NodeMaintenance
            properties:
              phase:
                description: Phase represents the current phase of the maintenance
                enum:
                - Pending
                - Ready
                - InProgress
                - Completed
                - Failed
                type: string
              message:
                description: Message provides additional information about the current phase
                type: string
              startTime:
                description: StartTime is the time when the maintenance started
                format: date-time
                type: string
              completionTime:
                description: CompletionTime is the time when the maintenance completed
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true 